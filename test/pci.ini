
[defaultDb]
driver = mysql
host = localhost
name = pci
user = root
password = root
charset = utf8

[log]
defaultLevel = debug
writer = sbronsted\SyslogWriter

[build]
root = /tmp/pci

[email]
from = 'root@dev.ufds.org';
; if to is set all mails are sent to this
to = '';
